#include <stdio.h>

/**
 * main - Prints the amount of arguments pased to the program
 * @argc: amount of arguments
 * @*argv[]: array of arguments
 * @argv: value of *argv
 * Return: 0
 */

int main(int argc, char *argv[])
{
	argv = argv;
	printf("%d\n", argc - 1);
	return (0);
}
